{
  "scripts": {
    "dev": "vitepress dev --port 3000",
    "build": "vitepress build",
    "lint": "prettier --check . && knip --no-config-hints",
    "preview": "vitepress preview",
    "test:e2e": "npm run build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "knip": "^6.35.1",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "vitepress": "^1.6.4"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
